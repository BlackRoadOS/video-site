{
  "name": "roadview-video",
  "version": "1.0.0",
  "private": true,
  "description": "RoadView — WebRTC video conferencing and screen sharing by BlackRoad OS",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
